# Sample Data

The sample database used by Access ALAYA is included inside the main source modules.

**Location:**  
`src/ALAYADB/`

This folder exists only as a placeholder to indicate that sample data is bundled within the framework and cannot run independently.
